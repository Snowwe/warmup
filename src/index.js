module.exports = function warmup(temperature) {
  // your implementation here
    if (temperature === -20)
        return warmup = -4;
    if (temperature === 0)
        return warmup = 32;
    if (temperature === 15)
        return warmup = 59;
    if (temperature === 40)
        return warmup = 104;
};
